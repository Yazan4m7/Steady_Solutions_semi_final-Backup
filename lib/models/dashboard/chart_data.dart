   import 'package:flutter/material.dart';

class ChartData {
        ChartData({this.x, this.y,this.color,this.text} );
             String? x;
             double? y;
             String? text;
             Color? color;
    }